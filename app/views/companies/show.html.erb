<div class="alert alert-block">
<button type="button" class="close" data-dismiss="alert">&times;</button>
  <h4>
    Please <%= link_to "Create a new process", new_target_path(:company_id => @company.id) %>
  </h4>
</div>
<p align=right>
  <%= link_to "New Process", new_target_path(:company_id => @company.id), class: "btn" %>
</p>
<p align=left>
  <b>Name:</b>
  <%= @company.name %>
</p>

<p>
  <b>Description:</b><br>
  <%= @company.description %>
</p>
<br>
    <% if current_user.admin? && current_user.company_id != nil %>

<p align=right>
  <%= link_to "Remove All Occurrences", remove_all_occurrences_path, :confirm => 'Are you sure?', :method => :get , class: "btn btn-danger" %>
</p>
<% end %>
